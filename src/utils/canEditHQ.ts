import { supabase } from "../lib/supabaseClient";export function canEditHQ(session: Session | null, role: string | null) {   if (!session) return false;   if (!role) return false;    return role === 'OWNER' || role === 'R5' || role === 'R4'; } 
